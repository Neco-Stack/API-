export default interface Kommentar {
    postId: number; 
    id: number; 
    email: string; 
    body: string; 
}
export default interface Post {
    userID: number; 
    id: number;
    title: string,
    body: string; 
}

